---
import ContactContent from '../../components/content/ContactContent.astro';

export async function getStaticPaths() {
  return [
    { params: { lang: 'en' } },
    { params: { lang: 'ca' } },
  ];
}

const { lang } = Astro.params;
---

<ContactContent lang={lang as 'en' | 'ca'} />
