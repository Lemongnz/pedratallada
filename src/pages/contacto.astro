---
import Layout from '../layouts/Layout.astro';
import { Image } from 'astro:assets';
import contactImg from '../assets/images/fort-bernia.jpg';
---

<Layout
	title="Contacto | Anthropos"
	description="Contacta con nosotros para organizar visitas, talleres o colaboraciones."
>
    <div class="grid md:grid-cols-2 gap-12 items-center min-h-[600px]">
        
        <!-- Contact Form -->
        <div class="order-2 md:order-1">
            <span class="text-clay font-bold tracking-widest text-sm uppercase mb-2 block">Hablemos</span>
            <h1 class="text-4xl md:text-5xl font-serif text-stone-900 mb-6">Contacta con nosotros</h1>
            <p class="text-stone-600 mb-8 text-lg">
                Estamos disponibles para consultas sobre proyectos educativos, visitas guiadas o propuestas de investigación.
            </p>

            <form class="space-y-6">
                <div class="grid md:grid-cols-2 gap-6">
                    <div>
                        <label for="name" class="block text-sm font-medium text-stone-700 mb-2">Nombre</label>
                        <input type="text" id="name" class="w-full px-4 py-3 rounded-lg bg-stone-50 border border-stone-200 focus:border-clay focus:ring-1 focus:ring-clay outline-none transition-colors" placeholder="Tu nombre" />
                    </div>
                    <div>
                        <label for="email" class="block text-sm font-medium text-stone-700 mb-2">Email</label>
                        <input type="email" id="email" class="w-full px-4 py-3 rounded-lg bg-stone-50 border border-stone-200 focus:border-clay focus:ring-1 focus:ring-clay outline-none transition-colors" placeholder="tu@email.com" />
                    </div>
                </div>
                <div>
                    <label for="subject" class="block text-sm font-medium text-stone-700 mb-2">Asunto</label>
                    <select id="subject" class="w-full px-4 py-3 rounded-lg bg-stone-50 border border-stone-200 focus:border-clay focus:ring-1 focus:ring-clay outline-none transition-colors">
                        <option>Información general</option>
                        <option>Presupuesto talleres</option>
                        <option>Visita a museos</option>
                        <option>Colaboración científica</option>
                    </select>
                </div>
                <div>
                    <label for="message" class="block text-sm font-medium text-stone-700 mb-2">Mensaje</label>
                    <textarea id="message" rows="4" class="w-full px-4 py-3 rounded-lg bg-stone-50 border border-stone-200 focus:border-clay focus:ring-1 focus:ring-clay outline-none transition-colors" placeholder="¿En qué podemos ayudarte?"></textarea>
                </div>
                <button type="submit" class="w-full bg-stone-900 text-white px-8 py-4 rounded-lg font-bold hover:bg-stone-800 transition-colors shadow-lg">
                    Enviar Mensaje
                </button>
            </form>
        </div>

        <!-- Image Side -->
        <div class="order-1 md:order-2 relative h-64 md:h-full min-h-[400px] rounded-3xl overflow-hidden shadow-2xl">
            <Image 
                src={contactImg} 
                alt="Fort de Bernia" 
                class="absolute inset-0 w-full h-full object-cover"
            />
            <div class="absolute bottom-0 left-0 right-0 p-8 bg-gradient-to-t from-stone-900/80 to-transparent">
                <p class="text-white font-medium text-sm">Vista desde el Fort de Bernia</p>
                <p class="text-stone-300 text-xs">Alicante, España</p>
            </div>
        </div>
    </div>
</Layout>
